application:
  some-property: testValue
spring:
  profiles:
      include: swagger
  data:
      mongodb:
          uri: mongodb://localhost:12345
          database: VtubeTest
s3:
  region: test
  accessKey: testkey
  secretKey: sectretKey